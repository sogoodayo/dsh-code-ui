# 项目规划（dsh-code-ui）

> 本文件用于承载产品开发规划与建议。**每次完成一项开发后更新本文件**，把对应项标记为「已完成」，并记录变更摘要。
>
> - 分支：`feature/code-editor`
> - 优先级图例：`🔴 高` / `🟠 中` / `🟡 低`

---

## 一、产品定位

一个运行在 DeepSeek Harness（DSH）里的 **Cursor 风格 AI 代码编辑器**。核心价值不是"做一个又一个编辑器功能"，而是：

> **把"代码选区"变成"AI 上下文"，让 AI 直接看到你正在思考的代码。**

当前已经从 `host.js` / `client.js` 实现了完整的编辑器工作台：文件树、多标签页、行号、状态栏、引用/备注/翻译、圆角卡片 AI 输入框、文件浏览器「以此文件夹为根」。引用机制（v1.1.3）：引用（给 AI）与翻译请求统一走 host 端引用存储 + AI 端 get_referenced_snippets 工具读取；输入框上方两种都只显示小标签（🔵 代码引用 / 🌐 翻译，均可 ✕ 删除），发送时不再把代码正文粘贴进消息，只附一行「[Code UI] 已标记 N 段代码引用、M 条翻译请求」指针，消息明面上不展示引用文本。

---

## 二、当前核心优势（必须保留）

1. **「代码选区 → AI 上下文」主线顺滑**：右键引用 → 发送自动附带完整代码 → AI 直接可见，零额外操作。
2. **内置编辑器**：文件树 + 多标签 + 行号 + 状态栏 + 窗口化。
3. **用 DSH 语义色变量**，自动跟随深浅主题。
4. **`get_referenced_snippets` 工具兜底**：不自动附带时，AI 也能按需取回引用。

---

## 三、当前短板（开发重点）

| 短板 | 影响 | 优先级 |
|---|---|---|
| 编辑器无语法高亮（纯 `<textarea>`） | 看代码累，"编辑器"而非"IDE" | 🔴 高 |
| 引用/备注只在内存，重启即失 | 工作流数据不持久 | 🔴 高 |
| 无代码补全 / 跳转 / 格式化 | 缺"智能"感 | 🟠 中 |
| 无新建/重命名/删除文件 | 文件树只能浏览+编辑已有文件 | 🟠 中 |
| 编辑器 diff/撤销历史受限 | textarea 原生撤销太弱 | 🟡 低 |
| 多会话/多工作区隔离不清 | "引用"共享范围语义模糊 | 🟡 低 |

---

## 四、建议路线图（按优先级 / 性价比）

### 🔴 P0 · 立刻做

1. **语法高亮**
   - 轻量：`highlight.js` / `prism` 只读着色层（透明 textarea + 下层 `<pre>`）。
   - 完整：**CodeMirror 6**（推荐）——可插拔、体积可控、无 React 依赖，自带行号/选区/撤销栈，可内嵌，无需重写架构。优先用它对 `<textarea>` 做替换。
2. **持久化引用/备注**
   - 把 Host 内存数组换成 DSH `storageDomain`（参考 `dsh-message-feedback`），落盘，重启不丢。

### 🟠 P1 · 立即可感知

3. **文件树完整操作**：新建文件/文件夹、重命名、删除（Host 加 RPC，沿用现有 `fs`）。
4. **一键格式化**：接入 Prettier（Host 端或浏览器端 `prettier/standalone`）。

### 🟡 P2 · 更像"AI 编码搭档"

5. **git diff 感知**：对比当前文件与磁盘/上个版本，AI 基于 diff 给建议。
6. **AI 主动写回**：AI 改完通过 `write-file` 落盘，加"应用 AI 建议"按钮。
7. **动作直达**：选中代码"解释 / 重构 / 找 bug"直接触发对应 AI 动作。

### ⚪ P3 · 打磨与工程化

8. **动态插件 → npm 持久化包**（`UPGRADE-TO-NPM.md` 已铺路），重启自动加载、可持续分发。
9. **调试面板**：暴露 RPC 调用日志、引用/备注视图、报错收集，辅助迭代。

---

## 五、核心建议（浓缩）

1. **语法高亮是第一优先级**——它是从"玩具编辑器"跨到"真编辑器"的分水岭，且现有文件树/标签/引用链路完好，只差这一步观感完全不同。用 **CodeMirror 6** 收益最大。
2. **持久化第二**——引用/备注是最强差异化能力，放内存太可惜，必须落盘。
3. **克制范围**——别急着做"代码补全/智能跳转"（那是 VS Code/Cursor 的地盘，精力有限拼不过），把"**选区即上下文 + 编辑器顺手 + 数据持久**"三件事做到极致，就是独特生态位。

---

## 六、开发进度跟踪

> 每完成一项，把对应行标记为 `✅ 已完成`，并加一行变更摘要。

| 项 | 优先级 | 状态 | 备注 |
|---|---|---|---|
| R-0 代码模块化重构（组件拆分、规范分层） | 🔴 | ✅ 已完成 | client.js 按 store/rpc/actions/pointer/components 分层，功能不变，commit b1bfa93 |
| P0-1 语法高亮（内联 Mini-Editor，替代 textarea） | 🔴 | 🚧 已完成实现，待实机验证 | overlay 方案：透明 textarea + 高亮 pre；自写 tokenizer；分支 feature/mini-editor。已修 2 bug：①CSS/SCSS 遇 `#` 死循环（# 分支不推进游标，现为十六进制颜色整体着色）；②文件异步读回后编辑区空白/与高亮层脱节（textarea 加 `loadSeq` 重挂载）。四处副本（lib、根 client.js、_cordis.client.txt、_legacy-src）同步修复 |
| P0-2 引用/备注持久化 | 🔴 | ✖️ 已取消 | 用户决策：不做持久化开发，重启后由 AI 重新加载插件即可。原动机（重启数据不丢）接受为已知限制 |
| P1-3 文件树新建/重命名/删除 | 🟠 | ⬜ 待做 | |
| P1-4 一键格式化（Prettier） | 🟠 | ⬜ 待做 | |
| P2-5 git diff 感知 | 🟡 | ⬜ 待做 | |
| P2-6 AI 主动写回 | 🟡 | ⬜ 待做 | |
| P2-7 选中动作直达 | 🟡 | ⬜ 待做 | |
| P3-8 转 npm 持久化包 | ⚪ | 🚧 进行中 | 源码已拆 src/host、src/client 多模块；tsdown 构建跑通可产出 lib/；分支 feature/npm-eng。实机验证发现并修复：①`connection.rpc.call` 返回 RpcResult 封套，客户端未解包导致全部 RPC 静默失效（文件树空/根按钮置灰/路径回车无效），已加 `callHost` 适配器；②`fs.stat` 返回 `FsInfo.type`（无 isDirectory/isFile 布尔字段），stat-path 按布尔字段判定目录恒 false，目录路径被误报「不存在」，已按 type 判定 + 客户端 exists 回退；③切根后空态反馈：编辑区空态显示当前工作区完整路径、侧边栏/窗口标题带根目录名、toast 改完整路径。均已同步安装副本并重打包 |
| P3-10 自更新卡片（检查更新/一键更新） | 🟢 | ✅ 已完成 | v1.1.1，分支 feature/self-update。设置→插件→插件配置卡片新增：版本自识（host 读自身 package.json，单点维护）、检查更新（updateRegistry 设置项为主源 + 另一源兜底，404 判「未发布」，严格 semver 防降级）、一键更新（registry 版本）与本地 .tgz 安装双通道、pnpm add 流式进度（--reporter=ndjson，实测事件名 packageId/stage/resolution，噪音 debug 事件丢弃）、失败自动归类建议 + minimumReleaseAge/瞬时网络自动重试、版本未变判定（pnpm 静默保旧版）、Windows cmd shim spawn + CI:true 防 pnpm 挂起 + taskkill 树杀 + 10min 超时。实现端到端实测（临时 profile 目录 pnpm add 本地 tgz 全链路 exit 0）+ 21 项功能单测。已同步安装副本并重打包 dsh-code-ui-1.1.0.tgz；host 半需重启 DSH 生效。v1.1.1 根因修复「卡片不显示」：插件配置页只分发 key ∈ host 已注册 settings 命名空间的卡片（ui-settings-plugins tab-store 交集逻辑），v1.1.0 只注册卡片未注册命名空间故永不渲染；照 dshmarket settings.ts 补 host 端 installSettingsSection（命名空间 dsh-code-ui，字段 updateRegistry 仅影响在线检查）+ dsh.client.inject 补 dsh-client-ui-settings 边 + dsh-settings 可选 peerDependency；新增 7 项 settings API 形状单测，重打包 1.1.1。v1.1.2 卡片样式统一：逐条对齐宿主 PluginCard.module.css / fields.module.css（dsh-context 卡片同源）--li 容器、12px 圆角 layer-3 背景、hover/open 态、14px×16px 头部（15px/600 名称 + 13px 描述两行 + 版本徽章 + IconChevronDownOutline14 旋转 chevron）、body 上边框左右 16px 缩进、字段行 12px 分隔、按钮 discard/save 样式、输入框 34px/8px 圆角；chevron 图标来自种子模块 @deepseek-ai/dsh-client-ui-primitives（与 react 同级可直接 require） |
| P3-9 调试面板 | ⚪ | ⬜ 待做 | |
